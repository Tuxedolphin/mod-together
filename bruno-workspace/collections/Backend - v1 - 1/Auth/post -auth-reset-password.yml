info:
  name: post /auth/reset-password
  type: http
  seq: 7
  tags:
    - Auth

http:
  method: POST
  url: "{{baseUrl}}/auth/reset-password"
  body:
    type: json
    data: |-
      {
        "tokenHash": "",
        "password": ""
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
