info:
  name: delete /profile/avatar
  type: http
  seq: 6
  tags:
    - Profile

http:
  method: DELETE
  url: "{{baseUrl}}/profile/avatar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Avatar deleted
    description: The avatar was removed, or no avatar was present.
    request:
      url: "{{baseUrl}}/profile/avatar"
      method: DELETE
    response:
      status: 204
      statusText: No Content
