info:
  name: delete /profile/me
  type: http
  seq: 7
  tags:
    - Profile

http:
  method: DELETE
  url: "{{baseUrl}}/profile/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Profile deleted
    description: The authenticated user's profile and auth account were deleted.
    request:
      url: "{{baseUrl}}/profile/me"
      method: DELETE
    response:
      status: 204
      statusText: No Content
