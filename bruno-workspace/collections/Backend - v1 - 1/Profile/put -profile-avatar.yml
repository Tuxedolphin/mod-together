info:
  name: put /profile/avatar
  type: http
  seq: 3
  tags:
    - Profile

http:
  method: PUT
  url: "{{baseUrl}}/profile/avatar"
  body:
    type: multipartForm
    multipartForm:
      - name: file
        type: file
        value: []
        enabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/profile/avatar"
      method: PUT
      body:
        type: multipartForm
        multipartForm:
          - name: file
            type: file
            value: []
            enabled: true
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "userId": "",
            "username": "",
            "avatarUrl": ""
          }
